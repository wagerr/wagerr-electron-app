export default {
  // This one is needed because vue-i18n still doesnt know that when using 'slots'  it should also fallback to key when key/value is missing
  'Jump to the {tab} and start placing bets': 'Jump to the {tab} and start placing bets',  
}